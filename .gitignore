node_modules/
dist/
test-results/
playwright-report/
blob-report/
playwright/.cache/
*.tsbuildinfo

# Secretos — nunca al repo
.env
.env.*
!.env.example
supabase/.temp/

# Builds de la app mobile (APK) — no van al repo, se generan localmente
apps/mobile/builds/
