import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "cl.domiciliovetvalpo.staff",
  appName: "Domicilio Vet Valpo Staff",
  webDir: "dist",
};

export default config;
